[build]
  command = "npm run build"
  publish = "out"
[functions]
  node_bundler = "esbuild"
  external_node_modules = ["@ampproject/toolbox-optimizer", "sharp", "next", "@prisma/client"]
[[headers]]
  for = "https://development.bereghici.dev/*"
  [headers.values]
    X-Robots-Tag = "noindex"
